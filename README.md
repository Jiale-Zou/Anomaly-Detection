The file 'anomaly_detect.py' was used to calculate the normal interval, which based on 2σ principle.
The file 'anomaly_detect.sql' runs behind the file named 'anomaly_detect.py', it queries other fields needed.
The file 'anomaly_analyse.py' is the final step, it used Adtributor and LMDI algorithm to analyse the root anomalies.
